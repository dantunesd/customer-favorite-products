const {
  NODE_ENV,
  APP_NAME,
  APP_PORT,
  LOG_LEVEL,
  DB_URL,
  PRODUCTS_API_URL,
} = process.env;

module.exports = {
  NODE_ENV,
  APP_NAME,
  APP_PORT,
  LOG_LEVEL,
  DB_URL,
  PRODUCTS_API_URL,
};
